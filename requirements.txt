# Core
discord.py>=2.3,<3.0
python-dotenv>=1.0

# Reddit public JSON feeds + SerpApi provider + web dashboard all use aiohttp.
aiohttp>=3.9

# Phase 3 (LLM SKU fallback) — only used when ANTHROPIC_API_KEY is set.
anthropic>=0.49
